---
description: Auto-Merge Mode — commit, push, PR, and merge each completed task to main (session-wide)
---

Invoke the `merge` skill (via the Skill tool) to turn on Auto-Merge Mode for this session, then follow the skill's instructions exactly.

If the `merge` skill is exposed under a directory-scoped name (e.g. when working inside a git worktree, like `.claude/worktrees/<name>:merge`), invoke that scoped name instead of the bare `merge`.
